
### 任务背景
你是一家电商公司的数据分析师，现有用户行为数据集（CSV 文件），包含以下字段：
- `duration`：浏览时长（分钟）
- `past_spend`：历史累计消费金额（元）
- `purchased`：是否购买（0/1）
- `amount`：本次消费金额（仅当 purchased=1 时有值，否则为 NaN）

你需要基于该数据集完成两个建模任务。

---

### 任务一：逻辑回归（分类）
1. 使用 `duration` 和 `past_spend` 作为特征，`purchased` 作为目标，训练一个逻辑回归模型，预测用户是否会购买。
2. 划分训练集和测试集，评估模型性能。选择合适的评估指标（准确率、精确率、召回率、F1、AUC 等），并解释为什么选择这些指标。

---

### 任务二：线性回归（回归）
1. 仅使用购买样本（`purchased=1`），用 `duration` 和 `past_spend` 预测 `amount`（本次消费金额）。
2. 划分训练集和测试集，训练线性回归模型。


---

### 附加要求
- 对特征进行适当的预处理（如标准化），并说明原因。
- 如果有必要，可以尝试特征工程（如多项式特征、交互项），比较效果。
- 简单总结模型的局限性，并提出可能的改进方向。

---

请根据以上任务，独立编写代码并输出结果。完成后再与标准答案对比，或提交给我进行点评。